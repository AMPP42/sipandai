// Temporary stub - will be reimplemented with new database structure

interface UserActivityLogsProps {
  userId?: string;
}

export default function UserActivityLogs({ userId }: UserActivityLogsProps) {
  return (
    <div className="p-8 text-center text-muted-foreground">
      <p>Log aktivitas user akan segera tersedia</p>
    </div>
  );
}
