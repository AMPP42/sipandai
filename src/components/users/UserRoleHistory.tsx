// Temporary stub - will be reimplemented with new database structure

interface UserRoleHistoryProps {
  userId: string;
}

export default function UserRoleHistory({ userId }: UserRoleHistoryProps) {
  return (
    <div className="p-8 text-center text-muted-foreground">
      <p>Riwayat role user akan segera tersedia</p>
    </div>
  );
}
